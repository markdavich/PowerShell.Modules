Each user will have a directory with their profiles for for:
- PowerShell 7 (`Microsoft.PowerShell_profile.ps1`)
- VS Code (`Microsoft.VSCode_profile.ps1`)
- Visual Studio (`Microsoft.VisualStudio_profile.ps1`)
- Other profiles

These need to be run after `.Bops.Lib!\.profile.ps1` run